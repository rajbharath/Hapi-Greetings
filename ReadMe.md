### Set nvm version (Loaded from .nvmrc within the project)
  - nvm use

### Install Global dependencies (for livereload)
  - Nodemon

### Install dependencies (for installing project dependencies)
  > npm install

### Start the server
  - nodemon server.js

